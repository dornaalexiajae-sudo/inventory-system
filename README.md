Install XAMPP
Start Apache and MySQL
Open phpMyAdmin
Create database named voting_system
Import voting_system.sql
Go to https://dev.mysql.com/downloads/connector/j/
Click “Select operating system”
Select “Platform Independent” and download it then unzip it 9.Open the NetBeans project
Click File -> Import Project -> From Zip -> then select the “VotingSystem (database).zip” file na naa sa gdrive
Right click the project -> then properties -> go to libraries -> then sa right side naay “Add JAR/Folder” i click na
After that you should be prompted to select a file so find the extracted msql connector, it should be on the downloads folder of your pc inside a folder called “mysql-connector-j-9.7.0”, if you find it open it until you see the “mysql-connector-j-9.7.0.jar” file, click that and then click open
Finally, run the project
